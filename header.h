#include <SDL/SDL.h>

// class Det
// {
	// private:
	// public:
		// Det(SDL_Surface*);
		
		
// };
// Det::Det(SDL_Surface *screen)
// {
	// swap = SDL_ConvertSurface( screen, screen->format, screen->flags );
// }
